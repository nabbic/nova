{
  "openapi": "3.1.0",
  "info": {
    "title": "Nova API",
    "description": "Nova Technical Due Diligence Platform API",
    "version": "2.0.0"
  },
  "paths": {
    "/api/version2": {
      "get": {
        "tags": ["Meta"],
        "summary": "Get API Version",
        "description": "Returns the current API version. No authentication required.",
        "operationId": "get_version_api_version2_get",
        "responses": {
          "200": {
            "description": "Successful response with API version",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VersionResponse"
                },
                "example": {
                  "version": "2.0.0"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "VersionResponse": {
        "type": "object",
        "title": "VersionResponse",
        "properties": {
          "version": {
            "type": "string",
            "title": "Version"
          }
        },
        "required": ["version"],
        "examples": [
          {
            "version": "2.0.0"
          }
        ]
      }
    }
  }
}
