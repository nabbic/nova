{
  "openapi": "3.1.0",
  "info": {
    "title": "Nova API",
    "description": "Nova Technical Due Diligence Platform API",
    "version": "2.0.0"
  },
  "paths": {
    "/api/version2": {
      "get": {
        "tags": ["health"],
        "summary": "Get API version (v2)",
        "description": "Returns the current API version. Public endpoint with no authentication required. Used as a factory smoke test.",
        "operationId": "get_version_v2_api_version2_get",
        "responses": {
          "200": {
            "description": "Successful response with API version",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VersionV2Response"
                },
                "example": {
                  "version": "2.0.0"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "VersionV2Response": {
        "type": "object",
        "title": "VersionV2Response",
        "properties": {
          "version": {
            "type": "string",
            "title": "Version"
          }
        },
        "required": ["version"],
        "examples": [
          {
            "version": "2.0.0"
          }
        ]
      }
    }
  }
}
